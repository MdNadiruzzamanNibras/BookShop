import loading from "../images/loading/200w.webp"

const Loading = () => {
    return (
        <div className="flex justify-center ">
           <img src={loading} alt="" /> 
        </div>
    );
};

export default Loading;